//in game class maybe should add the placetile method, or add it in player class, because
//every time we switch the player we have to place a tile on the board


public void placeTile(){
  //randomly get a tile and place on board, so the total number of tile reduce one
  title.remove();
}


// check game ends in game

public void endGame(){
  if(tile.isEmpty()){
    System.out.println("The Game Is Over");
    return;
  }
}
